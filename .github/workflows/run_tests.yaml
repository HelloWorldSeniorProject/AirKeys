name: Run Python Tests
on:
  push:
    branches:
      - testing
  pull_request:
    branches:
      - testing

jobs:
  test:
    name: Setup and Run Test Suite
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3
      - name: Build Dev Container & Run Tests
        uses: devcontainers/ci@v0.3
        with:
          imageName: python-container
          runCMD: test -a
          
      
